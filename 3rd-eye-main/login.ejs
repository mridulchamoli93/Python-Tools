<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #4e54c8, #8f94fb);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
        }
        .login-container {
            display: flex;
            justify-content: space-between;
            background-color: #1b1b2f;
            border-radius: 15px;
            padding: 30px;
            width: 900px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        .form-section {
            width: 40%;
            padding: 40px;
            background-color: #0f0f1f;
            border-radius: 10px;
        }
        .form-section h2 {
            margin-bottom: 20px;
        }
        .form-section input {
            width: 100%;
            padding: 15px;
            margin-bottom: 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
        }
        .form-section button {
            width: 100%;
            padding: 15px;
            background-color: #ff007a;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            color: white;
            cursor: pointer;
        }
        .form-section button:hover {
            background-color: #ff3b8f;
        }
        .welcome-section {
            display: flex;
            flex-direction: column;
            justify-content: center;
            text-align: center;
            width: 50%;
        }
        .welcome-section h1 {
            font-size: 48px;
            margin-bottom: 20px;
        }
        .welcome-section p {
            font-size: 18px;
            opacity: 0.8;
        }
        .error-message {
            color: red;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="form-section">
            <h2>Login</h2>
            <form id="loginForm" action="/auth/login" method="POST"> <!-- Updated Action -->
                <input type="text" name="username" placeholder="Username" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit">Login</button>
            </form>
            <% if (message) { %>
                <p class="error-message"><%= message %></p>
            <% } %>
        </div>
        <div class="welcome-section">
            <h1>Welcome.</h1>
            <p>Experience the power of the Third EYE. Login to continue.</p>
        </div>
    </div>
</body>
</html>